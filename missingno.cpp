//if sorted 
just check if  num == index+1 
also can use binary search to do in O(logn) time - if no!=index+1 move to right subarray

// unsorted array
 XOR of all array XOR with XOR of N natural nos == Missing no.

